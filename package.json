{
  "name": "funko-pop-collection",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://www.anbora.dk/funko-pop-collection",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build && cpx public/data build/data && cpx public/images build/images",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "cpx": "^1.5.0",
    "gh-pages": "^6.0.0"
  }
}
